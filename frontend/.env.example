# Backend API Configuration
VITE_BACKEND_API_URL=http://localhost:3000

# SerpAPI Configuration (for direct client-side calls if needed)
# Note: It's recommended to proxy all API calls through the backend
# VITE_SERPAPI_KEY=your_serpapi_key_here

# Environment
VITE_ENV=development
